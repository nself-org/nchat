name: CI

on:
  push:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22'

# Test Policy Reference: docs/TEST-POLICY.md
# All tests must comply with:
# 1. Deterministic seeding - no Math.random() without mocks
# 2. Wall-clock race prevention - jest.useFakeTimers() required for time-dependent tests
# 3. External network isolation - all HTTP/GraphQL/WebSocket calls must be mocked in unit tests
# 4. Database isolation - in-memory or Docker containers for test databases
# 5. Timeout limits - 5s for unit tests, 30s for integration tests, 60s for E2E tests
# Failing tests are triaged against policy violations before declaring "flaky"

# Monorepo Structure (v1.0.0+):
# - backend/ - nSelf CLI backend infrastructure
# - frontend/ - Multi-platform frontend (web/mobile/desktop)
#   - frontend/apps/web - Next.js web app
#   - frontend/packages/* - Shared packages (@nself-chat/*)
# Migration Status: Complete (web app in frontend/apps/web)

jobs:
  # Stage 0: Detect changes to determine which CI paths to execute
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
      web: ${{ steps.filter.outputs.web }}
      packages: ${{ steps.filter.outputs.packages }}
      config: ${{ steps.filter.outputs.config }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check path changes
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - '.backend/**'
            frontend:
              - 'frontend/**'
            web:
              - 'frontend/src/**'
              - 'frontend/public/**'
            packages:
              - 'frontend/src/**'
              - 'frontend/package.json'
              - 'frontend/pnpm-lock.yaml'
              - 'frontend/pnpm-workspace.yaml'
            config:
              - 'frontend/tsconfig.json'
              - 'frontend/jest.config.js'
              - 'frontend/next.config.js'
              - 'frontend/tailwind.config.ts'
              - '.github/workflows/**'

  # Stage 1: Linting and formatting (run in parallel)
  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.web == 'true' ||
      needs.detect-changes.outputs.frontend == 'true' ||
      needs.detect-changes.outputs.backend == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run ESLint
        run: HASURA_ADMIN_SECRET=ci-lint-placeholder-not-a-real-secret pnpm lint
        # next lint loads next.config.js which has a build gate requiring this env var.
        # The value here is a non-functional CI placeholder — no real Hasura instance
        # is used during linting.

      - name: Check formatting
        run: pnpm format:check
        continue-on-error: true

      # S09.T04 — Dummy buffer lint gate.
      # Fails if any production E2EE or GraphQL source contains a bare `new Uint8Array(N) //`
      # pattern without an immediate .fill() or assignment (zero-buffer sentinel).
      # Test files are excluded from this check.
      - name: Lint — no bare zero-buffer sentinels in E2EE/graphql production paths (S09.T04)
        run: |
          set +e
          HITS=$(grep -rn \
            --include="*.ts" --include="*.tsx" \
            --exclude-dir=node_modules \
            --exclude-dir=__tests__ \
            --exclude="*.test.ts" --exclude="*.test.tsx" --exclude="*.spec.ts" \
            'new Uint8Array([0-9]\+) //' \
            src/lib/e2ee/ src/lib/graphql/ 2>/dev/null)
          set -e
          if [ -n "$HITS" ]; then
            echo "BUILD GATE FIRED: bare zero-buffer sentinel(s) found in production E2EE/graphql paths:"
            echo "$HITS"
            echo "Use .fill(), crypto.getRandomValues(), or a meaningful initial value instead of a bare zero buffer with a comment."
            exit 1
          fi
          echo "Zero-buffer sentinel check: PASS"

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.web == 'true' ||
      needs.detect-changes.outputs.frontend == 'true' ||
      needs.detect-changes.outputs.packages == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run TypeScript check
        run: pnpm type-check

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.web == 'true' ||
      needs.detect-changes.outputs.frontend == 'true' ||
      needs.detect-changes.outputs.packages == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    timeout-minutes: 90
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run tests
        run: |
          NODE_OPTIONS='--max-old-space-size=4096' npx jest --no-coverage --forceExit --maxWorkers=2
        env:
          CI: true
          NEXT_PUBLIC_USE_DEV_AUTH: 'true'
          NEXT_PUBLIC_ENV: 'test'

  hygiene:
    name: Production Hygiene
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.web == 'true' ||
      needs.detect-changes.outputs.frontend == 'true' ||
      needs.detect-changes.outputs.backend == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check production code hygiene
        run: bash frontend/scripts/check-production-hygiene.sh
        continue-on-error: true

  # Stage 2: Security scanning (blocking gate)
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [detect-changes, lint, type-check]
    if: |
      always() &&
      !cancelled() &&
      (needs.detect-changes.outputs.web == 'true' ||
       needs.detect-changes.outputs.frontend == 'true' ||
       needs.detect-changes.outputs.packages == 'true')
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run dependency audit
        run: pnpm audit --audit-level=high
        continue-on-error: true

      - name: Run security check script
        working-directory: .
        run: bash frontend/scripts/security-check.sh
        continue-on-error: true
        env:
          NODE_ENV: production

  # Stage 3: Build (after all quality gates pass)
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [detect-changes, lint, type-check, test, hygiene, security]
    if: |
      always() &&
      !cancelled() &&
      needs.lint.result == 'success' &&
      needs.type-check.result == 'success' &&
      needs.test.result == 'success' &&
      (needs.hygiene.result == 'success' || needs.hygiene.result == 'skipped') &&
      (needs.security.result == 'success' || needs.security.result == 'skipped')
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Validate production environment
        run: pnpm validate:env:prod
        continue-on-error: true
        if: github.ref == 'refs/heads/main' || github.event_name == 'pull_request'
        env:
          NODE_ENV: 'production'
          NEXT_PUBLIC_ENV: 'production'

      - name: Build
        run: HASURA_ADMIN_SECRET=ci-build-placeholder-not-a-real-secret NEXT_PUBLIC_USE_DEV_AUTH=false pnpm build
        # Build gate in next.config.js requires HASURA_ADMIN_SECRET for production builds.
        # CI uses a non-functional placeholder — the app never connects to a real Hasura
        # instance during the build gate check.

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: build-output
          path: frontend/.next
          retention-days: 7
          if-no-files-found: warn

  # Stage 4: E2E tests (after successful build)
  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [build]
    continue-on-error: true # E2E tests are optional for now
    # Run E2E tests on main branch or when explicitly requested
    if: github.ref == 'refs/heads/main' || contains(github.event.head_commit.message, '[run-e2e]')
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: build-output
          path: frontend/.next

      - name: Run E2E tests
        run: pnpm test:e2e
        env:
          NEXT_PUBLIC_USE_DEV_AUTH: 'true'

      - name: Upload Playwright report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 30
