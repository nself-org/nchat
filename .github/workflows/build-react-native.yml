name: Build React Native

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Target platform'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - ios
          - android
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release
  workflow_call:
    inputs:
      platform:
        required: false
        type: string
        default: 'all'
      build_type:
        required: false
        type: string
        default: 'debug'

env:
  NODE_VERSION: '22'
  JAVA_VERSION: '17'
  RUBY_VERSION: '3.2'

jobs:
  build-ios:
    name: Build iOS
    runs-on: macos-latest
    if: inputs.platform == 'all' || inputs.platform == 'ios'
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ env.RUBY_VERSION }}
          bundler-cache: true
          working-directory: frontend/platforms/react-native/ios

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile --ignore-scripts
          cd platforms/react-native && pnpm install --frozen-lockfile --ignore-scripts

      - name: Install CocoaPods
        run: |
          cd platforms/react-native/ios
          bundle install
          bundle exec pod install

      - name: Build iOS (Debug)
        if: inputs.build_type == 'debug'
        run: |
          cd platforms/react-native/ios
          xcodebuild -workspace NselfChat.xcworkspace \
            -scheme NselfChat \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            build

      - name: Install Apple Certificate
        if: inputs.build_type == 'release'
        uses: apple-actions/import-codesign-certs@v7
        with:
          p12-file-base64: ${{ secrets.IOS_DIST_CERT }}
          p12-password: ${{ secrets.IOS_DIST_CERT_PASSWORD }}

      - name: Install Provisioning Profile
        if: inputs.build_type == 'release'
        uses: apple-actions/download-provisioning-profiles@v6
        with:
          bundle-id: 'com.nself.chat'
          issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
          api-key-id: ${{ secrets.APPSTORE_KEY_ID }}
          api-private-key: ${{ secrets.APPSTORE_PRIVATE_KEY }}

      - name: Build iOS (Release)
        if: inputs.build_type == 'release'
        run: |
          cd platforms/react-native/ios
          xcodebuild -workspace NselfChat.xcworkspace \
            -scheme NselfChat \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/NselfChat.xcarchive \
            archive

          xcodebuild -exportArchive \
            -archivePath build/NselfChat.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist ExportOptions.plist

      - name: Upload iOS artifact
        uses: actions/upload-artifact@v7
        with:
          name: rn-ios-${{ inputs.build_type }}
          path: |
            frontend/platforms/react-native/ios/build/export/*.ipa
            frontend/platforms/react-native/ios/build/Build/Products/Debug-iphonesimulator/*.app
          retention-days: 14

  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    if: inputs.platform == 'all' || inputs.platform == 'android'
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile --ignore-scripts
          cd platforms/react-native && pnpm install --frozen-lockfile --ignore-scripts

      - name: Build Android (Debug)
        if: inputs.build_type == 'debug'
        run: |
          cd platforms/react-native/android
          ./gradlew assembleDebug

      - name: Decode Keystore
        if: inputs.build_type == 'release'
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE }}" | base64 -d > platforms/react-native/android/app/release.keystore

      - name: Build Android (Release)
        if: inputs.build_type == 'release'
        run: |
          cd platforms/react-native/android
          ./gradlew assembleRelease bundleRelease
        env:
          KEYSTORE_FILE: release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

      - name: Upload Android artifact
        uses: actions/upload-artifact@v7
        with:
          name: rn-android-${{ inputs.build_type }}
          path: |
            frontend/platforms/react-native/android/app/build/outputs/apk/**/*.apk
            frontend/platforms/react-native/android/app/build/outputs/bundle/**/*.aab
          retention-days: 14

  combine:
    name: Combine Artifacts
    runs-on: ubuntu-latest
    needs: [build-ios, build-android]
    if: always() && (needs.build-ios.result == 'success' || needs.build-android.result == 'success')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: rn-*

      - name: List artifacts
        run: find artifacts -type f -name "*"

      - name: Upload combined artifact
        uses: actions/upload-artifact@v7
        with:
          name: rn-all-platforms-${{ inputs.build_type }}
          path: artifacts/
          retention-days: 14
