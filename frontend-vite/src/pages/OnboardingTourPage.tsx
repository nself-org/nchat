/**
 * Purpose:    Routing-skeleton page for "/onboarding/tour" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /onboarding/tour
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function OnboardingTourPage() {
  return <PlaceholderScreen title="Onboarding Tour" path="/onboarding/tour" />
}
