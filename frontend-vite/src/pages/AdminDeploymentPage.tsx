/**
 * Purpose:    Routing-skeleton page for "/admin/deployment" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/deployment
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminDeploymentPage() {
  return <PlaceholderScreen title="Admin Deployment" path="/admin/deployment" />
}
