/**
 * Purpose:    Routing-skeleton page for "/dev/templates" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/templates
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevTemplatesPage() {
  return <PlaceholderScreen title="Dev Templates" path="/dev/templates" />
}
