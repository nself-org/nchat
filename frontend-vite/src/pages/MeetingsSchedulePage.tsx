/**
 * Purpose:    Routing-skeleton page for "/meetings/schedule" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /meetings/schedule
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function MeetingsSchedulePage() {
  return <PlaceholderScreen title="Meetings Schedule" path="/meetings/schedule" />
}
