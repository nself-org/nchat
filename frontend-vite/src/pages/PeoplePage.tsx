/**
 * Purpose:    Routing-skeleton page for "/people" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /people
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function PeoplePage() {
  return <PlaceholderScreen title="People" path="/people" />
}
