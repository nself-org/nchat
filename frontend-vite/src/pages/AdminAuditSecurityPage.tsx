/**
 * Purpose:    Routing-skeleton page for "/admin/audit/security" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/audit/security
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminAuditSecurityPage() {
  return <PlaceholderScreen title="Admin Audit Security" path="/admin/audit/security" />
}
