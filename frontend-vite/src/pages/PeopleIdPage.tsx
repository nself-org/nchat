/**
 * Purpose:    Routing-skeleton page for "/people/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /people/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function PeopleIdPage() {
  return <PlaceholderScreen title="People Id" path="/people/:id" />
}
