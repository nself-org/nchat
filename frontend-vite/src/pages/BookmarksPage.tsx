/**
 * Purpose:    Routing-skeleton page for "/bookmarks" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /bookmarks
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function BookmarksPage() {
  return <PlaceholderScreen title="Bookmarks" path="/bookmarks" />
}
