/**
 * Purpose:    Routing-skeleton page for "/auth/signup" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/signup
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthSignupPage() {
  return <PlaceholderScreen title="Auth Signup" path="/auth/signup" />
}
