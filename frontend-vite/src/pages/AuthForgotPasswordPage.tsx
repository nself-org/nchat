/**
 * Purpose:    Routing-skeleton page for "/auth/forgot-password" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/forgot-password
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthForgotPasswordPage() {
  return <PlaceholderScreen title="Auth Forgot Password" path="/auth/forgot-password" />
}
