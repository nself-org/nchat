/**
 * Purpose:    Routing-skeleton page for "/admin" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminPage() {
  return <PlaceholderScreen title="Admin" path="/admin" />
}
