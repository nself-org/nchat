/**
 * Purpose:    Routing-skeleton page for "/admin/billing" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/billing
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminBillingPage() {
  return <PlaceholderScreen title="Admin Billing" path="/admin/billing" />
}
