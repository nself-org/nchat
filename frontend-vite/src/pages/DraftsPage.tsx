/**
 * Purpose:    Routing-skeleton page for "/drafts" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /drafts
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DraftsPage() {
  return <PlaceholderScreen title="Drafts" path="/drafts" />
}
