/**
 * Purpose:    Routing-skeleton page for "/auth/2fa-backup" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/2fa-backup
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function Auth2faBackupPage() {
  return <PlaceholderScreen title="Auth2fa Backup" path="/auth/2fa-backup" />
}
