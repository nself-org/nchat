/**
 * Purpose:    Routing-skeleton page for "/graphql-playground" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /graphql-playground
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function GraphqlPlaygroundPage() {
  return <PlaceholderScreen title="Graphql Playground" path="/graphql-playground" />
}
