/**
 * Purpose:    Routing-skeleton page for "/settings/security/pin-lock" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/security/pin-lock
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsSecurityPinLockPage() {
  return <PlaceholderScreen title="Settings Security Pin Lock" path="/settings/security/pin-lock" />
}
