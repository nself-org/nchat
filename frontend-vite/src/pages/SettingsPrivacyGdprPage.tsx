/**
 * Purpose:    Routing-skeleton page for "/settings/privacy/gdpr" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/privacy/gdpr
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsPrivacyGdprPage() {
  return <PlaceholderScreen title="Settings Privacy Gdpr" path="/settings/privacy/gdpr" />
}
