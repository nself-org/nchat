/**
 * Purpose:    Routing-skeleton page for "/settings/privacy/location" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/privacy/location
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsPrivacyLocationPage() {
  return <PlaceholderScreen title="Settings Privacy Location" path="/settings/privacy/location" />
}
