/**
 * Purpose:    Routing-skeleton page for "/admin/analytics" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/analytics
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminAnalyticsPage() {
  return <PlaceholderScreen title="Admin Analytics" path="/admin/analytics" />
}
