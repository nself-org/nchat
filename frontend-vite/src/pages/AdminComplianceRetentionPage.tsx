/**
 * Purpose:    Routing-skeleton page for "/admin/compliance/retention" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/compliance/retention
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminComplianceRetentionPage() {
  return <PlaceholderScreen title="Admin Compliance Retention" path="/admin/compliance/retention" />
}
