/**
 * Purpose:    Routing-skeleton page for "/onboarding/complete" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /onboarding/complete
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function OnboardingCompletePage() {
  return <PlaceholderScreen title="Onboarding Complete" path="/onboarding/complete" />
}
