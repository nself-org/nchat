/**
 * Purpose:    Routing-skeleton page for "/onboarding" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /onboarding
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function OnboardingPage() {
  return <PlaceholderScreen title="Onboarding" path="/onboarding" />
}
