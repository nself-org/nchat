/**
 * Purpose:    Routing-skeleton page for "/auth/callback-complete" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/callback-complete
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthCallbackCompletePage() {
  return <PlaceholderScreen title="Auth Callback Complete" path="/auth/callback-complete" />
}
