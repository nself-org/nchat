/**
 * Purpose:    Routing-skeleton page for "/settings/keyboard" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/keyboard
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsKeyboardPage() {
  return <PlaceholderScreen title="Settings Keyboard" path="/settings/keyboard" />
}
