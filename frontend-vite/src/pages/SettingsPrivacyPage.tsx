/**
 * Purpose:    Routing-skeleton page for "/settings/privacy" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/privacy
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsPrivacyPage() {
  return <PlaceholderScreen title="Settings Privacy" path="/settings/privacy" />
}
