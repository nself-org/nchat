/**
 * Purpose:    Routing-skeleton page for "/settings/data" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/data
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsDataPage() {
  return <PlaceholderScreen title="Settings Data" path="/settings/data" />
}
