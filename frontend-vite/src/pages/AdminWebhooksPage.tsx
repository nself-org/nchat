/**
 * Purpose:    Routing-skeleton page for "/admin/webhooks" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/webhooks
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminWebhooksPage() {
  return <PlaceholderScreen title="Admin Webhooks" path="/admin/webhooks" />
}
