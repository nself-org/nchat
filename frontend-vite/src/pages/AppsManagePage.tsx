/**
 * Purpose:    Routing-skeleton page for "/apps/manage" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /apps/manage
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AppsManagePage() {
  return <PlaceholderScreen title="Apps Manage" path="/apps/manage" />
}
