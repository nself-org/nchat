/**
 * Purpose:    Routing-skeleton page for "/auth/signin" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/signin
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthSigninPage() {
  return <PlaceholderScreen title="Auth Signin" path="/auth/signin" />
}
