/**
 * Purpose:    Routing-skeleton page for "/apps/:id/settings" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /apps/:id/settings
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AppsIdSettingsPage() {
  return <PlaceholderScreen title="Apps Id Settings" path="/apps/:id/settings" />
}
