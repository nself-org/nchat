/**
 * Purpose:    Routing-skeleton page for "/admin/audit" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/audit
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminAuditPage() {
  return <PlaceholderScreen title="Admin Audit" path="/admin/audit" />
}
