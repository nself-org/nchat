/**
 * Purpose:    Routing-skeleton page for "/saved" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /saved
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SavedPage() {
  return <PlaceholderScreen title="Saved" path="/saved" />
}
