/**
 * Purpose:    Routing-skeleton page for "/admin/moderation" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/moderation
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminModerationPage() {
  return <PlaceholderScreen title="Admin Moderation" path="/admin/moderation" />
}
