/**
 * Purpose:    Routing-skeleton page for "/api-docs/bots" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /api-docs/bots
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ApiDocsBotsPage() {
  return <PlaceholderScreen title="Api Docs Bots" path="/api-docs/bots" />
}
