/**
 * Purpose:    Routing-skeleton page for "/settings/theme" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/theme
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsThemePage() {
  return <PlaceholderScreen title="Settings Theme" path="/settings/theme" />
}
