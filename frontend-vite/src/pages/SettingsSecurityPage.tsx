/**
 * Purpose:    Routing-skeleton page for "/settings/security" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/security
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsSecurityPage() {
  return <PlaceholderScreen title="Settings Security" path="/settings/security" />
}
