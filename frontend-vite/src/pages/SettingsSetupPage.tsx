/**
 * Purpose:    Routing-skeleton page for "/settings/setup" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/setup
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsSetupPage() {
  return <PlaceholderScreen title="Settings Setup" path="/settings/setup" />
}
