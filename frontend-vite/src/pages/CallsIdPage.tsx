/**
 * Purpose:    Routing-skeleton page for "/calls/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /calls/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function CallsIdPage() {
  return <PlaceholderScreen title="Calls Id" path="/calls/:id" />
}
