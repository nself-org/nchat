/**
 * Purpose:    Routing-skeleton page for "/settings/notifications" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/notifications
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsNotificationsPage() {
  return <PlaceholderScreen title="Settings Notifications" path="/settings/notifications" />
}
