/**
 * Purpose:    Routing-skeleton page for "/admin/performance" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/performance
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminPerformancePage() {
  return <PlaceholderScreen title="Admin Performance" path="/admin/performance" />
}
