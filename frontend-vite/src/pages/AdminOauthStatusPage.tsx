/**
 * Purpose:    Routing-skeleton page for "/admin/oauth-status" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/oauth-status
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminOauthStatusPage() {
  return <PlaceholderScreen title="Admin Oauth Status" path="/admin/oauth-status" />
}
