/**
 * Purpose:    Routing-skeleton page for "/auth/magic-link" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/magic-link
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthMagicLinkPage() {
  return <PlaceholderScreen title="Auth Magic Link" path="/auth/magic-link" />
}
