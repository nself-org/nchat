/**
 * Purpose:    Routing-skeleton page for "/admin/settings" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/settings
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminSettingsPage() {
  return <PlaceholderScreen title="Admin Settings" path="/admin/settings" />
}
