/**
 * Purpose:    Routing-skeleton page for "/dev" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevPage() {
  return <PlaceholderScreen title="Dev" path="/dev" />
}
