/**
 * Purpose:    Routing-skeleton page for "/settings/appearance" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/appearance
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsAppearancePage() {
  return <PlaceholderScreen title="Settings Appearance" path="/settings/appearance" />
}
