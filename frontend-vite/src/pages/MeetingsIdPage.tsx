/**
 * Purpose:    Routing-skeleton page for "/meetings/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /meetings/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function MeetingsIdPage() {
  return <PlaceholderScreen title="Meetings Id" path="/meetings/:id" />
}
