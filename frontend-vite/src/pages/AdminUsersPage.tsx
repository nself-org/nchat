/**
 * Purpose:    Routing-skeleton page for "/admin/users" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/users
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminUsersPage() {
  return <PlaceholderScreen title="Admin Users" path="/admin/users" />
}
