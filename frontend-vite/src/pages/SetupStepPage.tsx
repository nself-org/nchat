/**
 * Purpose:    Routing-skeleton page for "/setup/:step" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /setup/:step
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SetupStepPage() {
  return <PlaceholderScreen title="Setup Step" path="/setup/:step" />
}
