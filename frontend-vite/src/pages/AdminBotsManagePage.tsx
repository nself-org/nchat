/**
 * Purpose:    Routing-skeleton page for "/admin/bots/manage" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/bots/manage
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminBotsManagePage() {
  return <PlaceholderScreen title="Admin Bots Manage" path="/admin/bots/manage" />
}
