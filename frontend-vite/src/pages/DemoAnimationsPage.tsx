/**
 * Purpose:    Routing-skeleton page for "/demo/animations" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /demo/animations
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DemoAnimationsPage() {
  return <PlaceholderScreen title="Demo Animations" path="/demo/animations" />
}
