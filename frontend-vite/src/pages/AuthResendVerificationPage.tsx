/**
 * Purpose:    Routing-skeleton page for "/auth/resend-verification" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/resend-verification
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthResendVerificationPage() {
  return <PlaceholderScreen title="Auth Resend Verification" path="/auth/resend-verification" />
}
