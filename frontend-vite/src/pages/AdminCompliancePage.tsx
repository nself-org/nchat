/**
 * Purpose:    Routing-skeleton page for "/admin/compliance" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/compliance
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminCompliancePage() {
  return <PlaceholderScreen title="Admin Compliance" path="/admin/compliance" />
}
