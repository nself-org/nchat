/**
 * Purpose:    Routing-skeleton page for "/settings/profile" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/profile
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsProfilePage() {
  return <PlaceholderScreen title="Settings Profile" path="/settings/profile" />
}
