/**
 * Purpose:    Routing-skeleton page for "/dev/components" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/components
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevComponentsPage() {
  return <PlaceholderScreen title="Dev Components" path="/dev/components" />
}
