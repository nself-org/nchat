/**
 * Purpose:    Routing-skeleton page for "/admin/import" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/import
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminImportPage() {
  return <PlaceholderScreen title="Admin Import" path="/admin/import" />
}
