/**
 * Purpose:    Routing-skeleton page for "/settings" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsPage() {
  return <PlaceholderScreen title="Settings" path="/settings" />
}
