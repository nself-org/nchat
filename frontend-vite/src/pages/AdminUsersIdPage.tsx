/**
 * Purpose:    Routing-skeleton page for "/admin/users/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/users/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminUsersIdPage() {
  return <PlaceholderScreen title="Admin Users Id" path="/admin/users/:id" />
}
