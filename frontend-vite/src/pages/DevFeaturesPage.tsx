/**
 * Purpose:    Routing-skeleton page for "/dev/features" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/features
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevFeaturesPage() {
  return <PlaceholderScreen title="Dev Features" path="/dev/features" />
}
