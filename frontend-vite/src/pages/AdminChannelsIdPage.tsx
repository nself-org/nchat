/**
 * Purpose:    Routing-skeleton page for "/admin/channels/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/channels/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminChannelsIdPage() {
  return <PlaceholderScreen title="Admin Channels Id" path="/admin/channels/:id" />
}
