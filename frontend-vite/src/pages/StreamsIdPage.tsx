/**
 * Purpose:    Routing-skeleton page for "/streams/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /streams/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function StreamsIdPage() {
  return <PlaceholderScreen title="Streams Id" path="/streams/:id" />
}
