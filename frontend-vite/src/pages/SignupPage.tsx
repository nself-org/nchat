/**
 * Purpose:    Routing-skeleton page for "/signup" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /signup
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SignupPage() {
  return <PlaceholderScreen title="Signup" path="/signup" />
}
