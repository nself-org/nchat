/**
 * Purpose:    Routing-skeleton page for "/admin/config" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/config
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminConfigPage() {
  return <PlaceholderScreen title="Admin Config" path="/admin/config" />
}
