/**
 * Purpose:    Routing-skeleton page for "/" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function HomePage() {
  return <PlaceholderScreen title="Home" path="/" />
}
