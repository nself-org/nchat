/**
 * Purpose:    Routing-skeleton page for "/invite/:code" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /invite/:code
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function InviteCodePage() {
  return <PlaceholderScreen title="Invite Code" path="/invite/:code" />
}
