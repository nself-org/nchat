/**
 * Purpose:    Routing-skeleton page for "/admin/social" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/social
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminSocialPage() {
  return <PlaceholderScreen title="Admin Social" path="/admin/social" />
}
