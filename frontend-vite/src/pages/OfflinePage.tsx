/**
 * Purpose:    Routing-skeleton page for "/offline" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /offline
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function OfflinePage() {
  return <PlaceholderScreen title="Offline" path="/offline" />
}
