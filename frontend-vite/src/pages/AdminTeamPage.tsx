/**
 * Purpose:    Routing-skeleton page for "/admin/team" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/team
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminTeamPage() {
  return <PlaceholderScreen title="Admin Team" path="/admin/team" />
}
