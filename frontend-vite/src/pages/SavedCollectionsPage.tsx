/**
 * Purpose:    Routing-skeleton page for "/saved/collections" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /saved/collections
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SavedCollectionsPage() {
  return <PlaceholderScreen title="Saved Collections" path="/saved/collections" />
}
