/**
 * Purpose:    Routing-skeleton page for "/channels/browse" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /channels/browse
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ChannelsBrowsePage() {
  return <PlaceholderScreen title="Channels Browse" path="/channels/browse" />
}
