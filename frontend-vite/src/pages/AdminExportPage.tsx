/**
 * Purpose:    Routing-skeleton page for "/admin/export" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/export
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminExportPage() {
  return <PlaceholderScreen title="Admin Export" path="/admin/export" />
}
