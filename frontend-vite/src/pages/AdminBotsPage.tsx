/**
 * Purpose:    Routing-skeleton page for "/admin/bots" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/bots
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminBotsPage() {
  return <PlaceholderScreen title="Admin Bots" path="/admin/bots" />
}
