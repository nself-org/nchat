/**
 * Purpose:    Routing-skeleton page for "/dev/components/users" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/components/users
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevComponentsUsersPage() {
  return <PlaceholderScreen title="Dev Components Users" path="/dev/components/users" />
}
