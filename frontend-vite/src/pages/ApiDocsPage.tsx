/**
 * Purpose:    Routing-skeleton page for "/api-docs" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /api-docs
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ApiDocsPage() {
  return <PlaceholderScreen title="Api Docs" path="/api-docs" />
}
