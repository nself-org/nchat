/**
 * Purpose:    Routing-skeleton page for "/setup" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /setup
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SetupPage() {
  return <PlaceholderScreen title="Setup" path="/setup" />
}
