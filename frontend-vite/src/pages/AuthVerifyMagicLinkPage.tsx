/**
 * Purpose:    Routing-skeleton page for "/auth/verify-magic-link" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/verify-magic-link
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthVerifyMagicLinkPage() {
  return <PlaceholderScreen title="Auth Verify Magic Link" path="/auth/verify-magic-link" />
}
