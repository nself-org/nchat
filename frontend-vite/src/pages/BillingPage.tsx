/**
 * Purpose:    Routing-skeleton page for "/billing" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /billing
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function BillingPage() {
  return <PlaceholderScreen title="Billing" path="/billing" />
}
