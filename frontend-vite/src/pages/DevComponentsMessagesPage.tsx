/**
 * Purpose:    Routing-skeleton page for "/dev/components/messages" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/components/messages
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevComponentsMessagesPage() {
  return <PlaceholderScreen title="Dev Components Messages" path="/dev/components/messages" />
}
