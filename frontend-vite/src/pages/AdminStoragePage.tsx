/**
 * Purpose:    Routing-skeleton page for "/admin/storage" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/storage
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminStoragePage() {
  return <PlaceholderScreen title="Admin Storage" path="/admin/storage" />
}
