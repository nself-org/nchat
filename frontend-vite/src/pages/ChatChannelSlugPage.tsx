/**
 * Purpose:    Routing-skeleton page for "/chat/channel/:slug" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /chat/channel/:slug
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ChatChannelSlugPage() {
  return <PlaceholderScreen title="Chat Channel Slug" path="/chat/channel/:slug" />
}
