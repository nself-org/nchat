/**
 * Purpose:    Routing-skeleton page for "/admin/messages/history" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/messages/history
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminMessagesHistoryPage() {
  return <PlaceholderScreen title="Admin Messages History" path="/admin/messages/history" />
}
