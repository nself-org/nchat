/**
 * Purpose:    Routing-skeleton page for "/settings/accessibility" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/accessibility
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsAccessibilityPage() {
  return <PlaceholderScreen title="Settings Accessibility" path="/settings/accessibility" />
}
