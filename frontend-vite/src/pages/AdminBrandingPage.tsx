/**
 * Purpose:    Routing-skeleton page for "/admin/branding" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/branding
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminBrandingPage() {
  return <PlaceholderScreen title="Admin Branding" path="/admin/branding" />
}
