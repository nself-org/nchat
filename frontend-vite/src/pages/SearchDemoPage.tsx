/**
 * Purpose:    Routing-skeleton page for "/search-demo" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /search-demo
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SearchDemoPage() {
  return <PlaceholderScreen title="Search Demo" path="/search-demo" />
}
