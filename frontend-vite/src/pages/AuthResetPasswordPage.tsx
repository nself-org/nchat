/**
 * Purpose:    Routing-skeleton page for "/auth/reset-password" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/reset-password
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthResetPasswordPage() {
  return <PlaceholderScreen title="Auth Reset Password" path="/auth/reset-password" />
}
