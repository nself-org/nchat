/**
 * Purpose:    Routing-skeleton page for "/apps/:id" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /apps/:id
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AppsIdPage() {
  return <PlaceholderScreen title="Apps Id" path="/apps/:id" />
}
