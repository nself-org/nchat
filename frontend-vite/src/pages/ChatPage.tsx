/**
 * Purpose:    Routing-skeleton page for "/chat" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /chat
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ChatPage() {
  return <PlaceholderScreen title="Chat" path="/chat" />
}
