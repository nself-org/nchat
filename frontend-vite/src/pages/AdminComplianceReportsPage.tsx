/**
 * Purpose:    Routing-skeleton page for "/admin/compliance/reports" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/compliance/reports
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminComplianceReportsPage() {
  return <PlaceholderScreen title="Admin Compliance Reports" path="/admin/compliance/reports" />
}
