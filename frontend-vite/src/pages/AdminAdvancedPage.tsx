/**
 * Purpose:    Routing-skeleton page for "/admin/advanced" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/advanced
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminAdvancedPage() {
  return <PlaceholderScreen title="Admin Advanced" path="/admin/advanced" />
}
