/**
 * Purpose:    Routing-skeleton page for "/apps/installed" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /apps/installed
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AppsInstalledPage() {
  return <PlaceholderScreen title="Apps Installed" path="/apps/installed" />
}
