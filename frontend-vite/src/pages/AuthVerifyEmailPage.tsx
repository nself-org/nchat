/**
 * Purpose:    Routing-skeleton page for "/auth/verify-email" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/verify-email
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AuthVerifyEmailPage() {
  return <PlaceholderScreen title="Auth Verify Email" path="/auth/verify-email" />
}
