/**
 * Purpose:    Routing-skeleton page for "/dev/components/channels" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /dev/components/channels
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DevComponentsChannelsPage() {
  return <PlaceholderScreen title="Dev Components Channels" path="/dev/components/channels" />
}
