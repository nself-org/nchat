/**
 * Purpose:    Routing-skeleton page for "/settings/account" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/account
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsAccountPage() {
  return <PlaceholderScreen title="Settings Account" path="/settings/account" />
}
