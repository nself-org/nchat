/**
 * Purpose:    Routing-skeleton page for "/auth/2fa-verify" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /auth/2fa-verify
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function Auth2faVerifyPage() {
  return <PlaceholderScreen title="Auth2fa Verify" path="/auth/2fa-verify" />
}
