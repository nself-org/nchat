/**
 * Purpose:    Routing-skeleton page for "/meetings" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /meetings
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function MeetingsPage() {
  return <PlaceholderScreen title="Meetings" path="/meetings" />
}
