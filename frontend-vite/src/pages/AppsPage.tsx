/**
 * Purpose:    Routing-skeleton page for "/apps" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /apps
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AppsPage() {
  return <PlaceholderScreen title="Apps" path="/apps" />
}
