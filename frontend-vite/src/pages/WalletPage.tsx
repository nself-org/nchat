/**
 * Purpose:    Routing-skeleton page for "/wallet" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /wallet
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function WalletPage() {
  return <PlaceholderScreen title="Wallet" path="/wallet" />
}
