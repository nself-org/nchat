/**
 * Purpose:    Routing-skeleton page for "/demo" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /demo
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function DemoPage() {
  return <PlaceholderScreen title="Demo" path="/demo" />
}
