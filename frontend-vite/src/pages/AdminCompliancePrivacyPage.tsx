/**
 * Purpose:    Routing-skeleton page for "/admin/compliance/privacy" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/compliance/privacy
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminCompliancePrivacyPage() {
  return <PlaceholderScreen title="Admin Compliance Privacy" path="/admin/compliance/privacy" />
}
