/**
 * Purpose:    Routing-skeleton page for "/test-env" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /test-env
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function TestEnvPage() {
  return <PlaceholderScreen title="Test Env" path="/test-env" />
}
