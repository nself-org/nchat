/**
 * Purpose:    Routing-skeleton page for "/admin/channels" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /admin/channels
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function AdminChannelsPage() {
  return <PlaceholderScreen title="Admin Channels" path="/admin/channels" />
}
