/**
 * Purpose:    Routing-skeleton page for "/activity" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /activity
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function ActivityPage() {
  return <PlaceholderScreen title="Activity" path="/activity" />
}
