/**
 * Purpose:    Routing-skeleton page for "/settings/storage" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /settings/storage
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function SettingsStoragePage() {
  return <PlaceholderScreen title="Settings Storage" path="/settings/storage" />
}
