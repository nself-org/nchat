/**
 * Purpose:    Routing-skeleton page for "/login" (migrated from legacy frontend/src/app).
 *             Renders the AsyncScreen empty state until the legacy page is ported.
 * SOT:        F-NCHAT-VITE-ROUTE — /login
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen'

export default function LoginPage() {
  return <PlaceholderScreen title="Login" path="/login" />
}
