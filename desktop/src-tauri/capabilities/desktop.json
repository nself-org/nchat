{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "desktop",
  "description": "Desktop-only capability for nChat — grants tray icon, file drag-and-drop, and system tray interactions. Applied only on macOS/Windows/Linux desktop targets.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "core:tray:default"
  ]
}
