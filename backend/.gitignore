# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║          NCHAT BACKEND .GITIGNORE                                         ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ============================================================================
# SECRETS - NEVER COMMIT
# ============================================================================
.env.secrets
.env.secrets.*
.env.local
.env.runtime
*.pem
*.key
*.crt
credentials.json
service-account.json

# ============================================================================
# RUNTIME STATE
# ============================================================================
.nself/build-state/
.nself/cache/

# ============================================================================
# VOLUMES & DATA
# ============================================================================
.volumes/

# ============================================================================
# LOGS
# ============================================================================
logs/*
!logs/.gitkeep

# ============================================================================
# BACKUPS
# ============================================================================
_backups/*
!_backups/.gitkeep
*.sql.backup
*.dump

# ============================================================================
# SSL CERTIFICATES (generated)
# ============================================================================
ssl/certificates/*
!ssl/certificates/.gitkeep
!ssl/.gitkeep

# ============================================================================
# TYPES (generated)
# ============================================================================
types/*
!types/.gitkeep

# ============================================================================
# IDE & OS
# ============================================================================
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# ============================================================================
# GENERATED FILES (nself CLI generates these - never commit)
# ============================================================================
# docker-compose.yml is tracked for CI (E2E tests need the compose to provision
# the backend stack; the file is regenerated by `nself build` locally).
# !docker-compose.yml keeps the CI-pinned version without affecting local
# `nself build` which overwrites it anyway.
docker-compose.*.yml
!docker-compose.yml

# ============================================================================
# KEEP THESE (do NOT ignore)
# ============================================================================
# !.env             # Main config (contains no secrets)
# !.env.example
# !.env.staging
# !.env.prod
# !schema.dbml
# !README.md
# !nself/migrations/
# !hasura/
# !nginx/
# !postgres/
# !auth/

# ============================================================================
# GENERATED ENV FILES (nself CLI generates these - never commit)
# ============================================================================
.env.runtime
.env.computed

