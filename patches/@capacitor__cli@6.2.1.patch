diff --git a/dist/util/template.js b/dist/util/template.js
index 5d41a32dac66b3988189c7067eee84c875c5cc21..64abc6fb998b777cd8fd7b53b374c74c15fe23fe 100644
--- a/dist/util/template.js
+++ b/dist/util/template.js
@@ -1,11 +1,14 @@
 "use strict";
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.extractTemplate = void 0;
-const tslib_1 = require("tslib");
 const utils_fs_1 = require("@ionic/utils-fs");
-const tar_1 = tslib_1.__importDefault(require("tar"));
+const tar_1 = require("tar");
 async function extractTemplate(src, dir) {
     await (0, utils_fs_1.mkdirp)(dir);
-    await tar_1.default.extract({ file: src, cwd: dir });
+    // tar 7 exposes `extract` directly on the module (no default export);
+    // tar 6 exposed both. Use the named export so this works with the
+    // security-patched tar >= 7.5.11 the repo pins globally.
+    const extract = tar_1.extract || (tar_1.default && tar_1.default.extract);
+    await extract({ file: src, cwd: dir });
 }
 exports.extractTemplate = extractTemplate;
