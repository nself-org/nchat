import { jsx as _jsx } from "react/jsx-runtime";
/**
 * Stub button component — stub only until S05 port.
 * Full implementation with CVA variants lands in S05.
 */
export const Button = ({ children, onClick, className }) => (_jsx("button", { onClick: onClick, className: className, children: children }));
